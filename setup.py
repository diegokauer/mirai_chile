from setuptools import setup, find_packages

setup(
    name="mirai_chile",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, e.g.,
        # 'torch>=1.11.0',
    ],
)